# Le clone Buildroot et tout ce qu'il génère (sources téléchargées,
# toolchain compilée, rootfs, image finale) ne doivent jamais être commités :
# plusieurs Go, régénérés à chaque compilation (locale ou via GitHub Actions).
/buildroot/

# Fichiers temporaires courants
*.o
*.a
__pycache__/
*.pyc
.DS_Store
