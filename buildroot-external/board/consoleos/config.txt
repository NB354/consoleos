# ConsoleOS - config.txt Raspberry Pi 4
arm_64bit=1
kernel=Image
device_tree=bcm2711-rpi-4-b.dtb

# Boot rapide : pas d'écran de warning, pas de délai
disable_splash=1
boot_delay=0
avoid_warnings=1

# Mémoire GPU suffisante pour la sortie 1080p60 et l'accélération
gpu_mem=76

# Force le HDMI même sans écran détecté au boot (évite un échec silencieux)
hdmi_force_hotplug=1
hdmi_group=1
hdmi_mode=16

# USB / manettes
dtoverlay=dwc2,dr_mode=host

# Bluetooth activé (manettes sans fil)
dtparam=krnbt=on
